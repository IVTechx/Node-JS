# Node-JS

Handling Extensionless Files
  Default Behavior: Files without an extension (e.g., README, Makefile) are automatically moved into a folder named "others".
  Customization: To ignore extensionless files instead of sorting them, swap the logic on lines 15 and 16.
